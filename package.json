{
  "name": "mabrur",
  "private": true,
  "workspaces": [
    "server"
  ],
  "scripts": {
    "server:dev": "npm run dev -w server",
    "server:build": "npm run build -w server",
    "db:migrate": "npm run db:migrate -w server",
    "db:rollback": "npm run db:rollback -w server",
    "db:seed": "npm run db:seed -w server",
    "test": "npm run test -w server",
    "db:cleanup": "npm run db:cleanup -w server",
    "simulate": "tsx docs/accuracy-test/run.ts && tsx docs/accuracy-test/verify-manuscript.ts"
  }
}
